# Optional — not needed if you use the combined deploy on flaudaa (main_flaudaa.yml).
# Website is served from the API app: https://flaudaa-dwf9bhg5e8g6bebv.canadacentral-01.azurewebsites.net/

name: (optional) flaudaa-web — use main_flaudaa.yml instead

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy the site via main_flaudaa.yml to flaudaa only. See docs/SETUP-SIMPLE.md."
